

## Remover duplicata restante do cftvWork5

O diff anterior removeu apenas uma das duplicatas, mas `cftvWork5` ainda aparece duas vezes no array. Vou remover a linha duplicada restante, mantendo apenas uma entrada de `cftvWork5`.

### Alteracao

**`src/components/ServicesSection.tsx`**: Remover a segunda ocorrencia de `cftvWork5` no array de imagens do CFTV, deixando apenas uma.

